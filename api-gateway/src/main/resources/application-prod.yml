logging.level:
  root: WARN
  com.example.banking: INFO
gateway.security:
  allowed-origins:
    - ${APP_ALLOWED_ORIGIN}
  jwt:
    issuer: ${JWT_ISSUER:banking-identity}
    secret: ${JWT_SECRET}
    ttl: PT20M
